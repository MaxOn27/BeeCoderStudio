import React from 'react';
import { Spinner } from "@material-tailwind/react";
const LoadingSpinner = () => {
  return <Spinner className="h-16 w-16 text-blue-gray-700" />;
};

export default LoadingSpinner;